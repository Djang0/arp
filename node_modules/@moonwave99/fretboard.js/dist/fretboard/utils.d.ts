import { Selection, BaseType } from 'd3-selection';
import { Position, Options } from './Fretboard';
export declare function getStringThickness({ stringWidth, stringIndex }: {
    stringWidth: number | [number];
    stringIndex: number;
}): number;
export declare function generateStrings({ stringCount, stringWidth, height }: {
    stringCount: number;
    stringWidth: number | [number];
    height: number;
}): number[];
export declare function generateFrets({ scaleFrets, fretCount }: {
    scaleFrets: boolean;
    fretCount: number;
}): number[];
export declare function dotClasses(dot: Position, prefix?: string): string;
export declare function getDimensions({ topPadding, bottomPadding, leftPadding, rightPadding, width, height, showFretNumbers, fretNumbersHeight }: {
    topPadding: number;
    bottomPadding: number;
    leftPadding: number;
    rightPadding: number;
    width: number;
    height: number;
    showFretNumbers: boolean;
    fretNumbersHeight: number;
}): {
    totalWidth: number;
    totalHeight: number;
};
declare type GetPositionParams = {
    event: MouseEvent;
    stringsGroup: Selection<BaseType, unknown, HTMLElement, unknown>;
    leftPadding: number;
    nutWidth: number;
    strings: number[];
    frets: number[];
    dots: Position[];
};
export declare const getPositionFromMouseCoords: ({ event, stringsGroup, leftPadding, nutWidth, strings, frets, dots }: GetPositionParams) => Position;
export declare function createHoverDiv({ bottomPadding, showFretNumbers, fretNumbersHeight }: Options): HTMLDivElement;
export {};
