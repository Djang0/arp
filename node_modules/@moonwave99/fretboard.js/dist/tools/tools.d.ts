import { Position } from '../fretboard/Fretboard';
export declare function disableStrings({ box, strings }: {
    box: Position[];
    strings: number[];
}): Position[];
export declare function sliceBox({ box, from, to }?: {
    box?: Position[];
    from?: {
        string: number;
        fret: number;
    };
    to?: {
        string: number;
        fret: number;
    };
}): Position[];
export declare function disableDots({ box, from, to }?: {
    box?: Position[];
    from?: {
        string: number;
        fret: number;
    };
    to?: {
        string: number;
        fret: number;
    };
}): Position[];
