import { Position } from '../../fretboard/Fretboard';
export declare enum Systems {
    pentatonic = "pentatonic",
    CAGED = "CAGED",
    TNPS = "TNPS"
}
declare type GetBoxParams = {
    root: string;
    box: number | string;
    mode?: number | string;
    system: Systems;
};
export declare function getModeFromScaleType(type: string): number;
export declare function getBox({ root, mode, system, box }: GetBoxParams): Position[];
export {};
