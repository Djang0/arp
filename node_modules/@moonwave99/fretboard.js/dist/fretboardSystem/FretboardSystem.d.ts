import { Systems } from './systems/systems';
import { Position, Tuning } from '../fretboard/Fretboard';
export declare type FretboardPosition = {
    string: number;
    fret: number;
    chroma: number;
};
declare type FretboardSystemParams = {
    tuning?: Tuning;
    fretCount?: number;
};
declare type ScaleParams = {
    type: string;
    root: string;
    box?: {
        system: Systems;
        box: string | number;
    };
};
export declare function isPositionInBox({ fret, string }: Position, systemPositions: Position[]): boolean;
export declare class FretboardSystem {
    private tuning;
    private fretCount;
    private positions;
    private baseNote;
    private baseOctave;
    constructor(params?: FretboardSystemParams);
    getTuning(): Tuning;
    getFretCount(): number;
    getNoteAtPosition(position: Position): string;
    getScale({ type, root: paramsRoot, box }: ScaleParams): Position[];
    private adjustOctave;
    private populate;
    private getOctave;
}
export {};
